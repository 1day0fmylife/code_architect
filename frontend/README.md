Hermes web console using Vite + React + Tailwind.

Install and run with pinned dependencies:

```bash
cd frontend
bun install --frozen-lockfile
bun run dev
```

Open `http://localhost:5173`, set the Hermes API URL and save `WEB_AUTH_TOKEN`.
